{
    "name": "express",
    "version": "1.0.0",
    "description": "",
    "main": "app.js",
    "scripts": {
      "dev": "nodemon --env-file=.env app.js ",
      "test": "echo \"Error: no test specified\" && exit 1"
      
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
      "express": "^4.18.2",
      "nodemon": "^3.0.3"
    }
  }
  
  /*
  {
      // "test1": "dotenv -e .env.sample -- jest -i",
      // "start-1": "NODE_ENV=development PORT=80 node ./bin/www",
      // "start-prod": "NODE_ENV=production PORT=81 node ./bin/www",
      // "migrate:postgres": "dotenv -e .env.sample -- npx prisma migrate deploy"
  }
  */